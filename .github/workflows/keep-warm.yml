name: Keep Twikoo Warm

on:
  schedule:
    - cron: "*/5 * * * *"

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Twikoo
        run: curl -s -o /dev/null -w "%{http_code}" -X POST https://twikoo.yujingblog.top/
